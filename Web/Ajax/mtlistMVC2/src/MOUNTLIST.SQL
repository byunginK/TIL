CREATE SEQUENCE MOUNTNUM
INCREMENT BY 1           
START WITH 1            
MAXVALUE 100;





CREATE table MOUNTLIST(
	MOUNTNUM NUMBER(30),
	LOCATION VARCHAR2(40) NOT NULL,
	NAME VARCHAR2(40) NOT NULL,
	HEIGHT NUMBER(30) NOT NULL,
	MANAGE VARCHAR2(40)
);

INSERT into MOUNTLIST
values(MOUNTNUM.NEXTVAL,'전라도','무등산',1187,'국립공원');

select * from MOUNTLIST;
	